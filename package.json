{
  "name": "gagged-notepasser",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Cloudflare Worker for GAGGED AI key custody and provider routing.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

